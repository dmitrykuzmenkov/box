check process redis
  with pidfile "/home/box/redis/redis.pid"
  start program = "/usr/bin/bash -l -c 'redis-server /home/box/redis/redis.conf'"
  stop program = "/usr/bin/bash -l -c 'kill `cat /home/box/redis/redis.pid`'"
  if not exist then alert
